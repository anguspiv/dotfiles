# yaml-language-server: $schema=https://raw.githubusercontent.com/JanDeDobbeleer/oh-my-posh/main/themes/schema.json

blocks:
  - alignment: left
    newline: true
    type: prompt
    segments:
      - type: text
        style: plain
        template: "{{if gt .Code -1}}\u0009{{end}}"
  - alignment: left
    newline: true
    segments:
      - background: transparent
        foreground: "#dd1e1e"
        style: plain
        template: " ⚡"
        type: root
      # - background: "#000000"
      #   foreground: "#ffffff"
      #   properties:
      #     alpine: 
      #     arch: 󰣇
      #     centos: 
      #     debian: 
      #     elementary: 
      #     fedora: 
      #     gentoo: 󰣨
      #     linux: 
      #     macos: 
      #     manjaro: 
      #     mint: 󰣭
      #     opensuse: 
      #     raspbian: 
      #     ubuntu: 
      #     windows: 
      #     wsl: 
      #   style: plain
      #   template: " {{ if .WSL }}WSL at {{ end }}{{.Icon}} "
      #   type: os
      - background: transparent
        foreground: "#43CCEA"
        properties:
          folder_icon: 󰉋
          folder_separator_icon: " <#000000> </>"
          home_icon: 󰋜
          style: folder
        style: plain
        template: "{{ .Path }} "
        type: path
      - background: transparent
        foreground: "#00ff0d"
        properties:
          fetch_stash_count: true
          fetch_status: true
        style: plain
        template: "<#ffffff> </>{{ .HEAD }}{{ if .Staging.Changed }}<#FF6F00>  {{ .Staging.String }}</>{{ end }}{{ if and (.Working.Changed) (.Staging.Changed) }} |{{ end }}{{ if .Working.Changed }}  {{ .Working.String }}{{ end }}{{ if gt .StashCount 0 }}  {{ .StashCount }}{{ end }} "
        type: git
      - type: node
        style: plain
        foreground: "#6CA35E"
        background: transparent
        foreground_templates:
          - "{{ if .Mismatch }}#ff0000{{ end }}"
        template: "<#ffffff> </>{{if .PackageManagerIcon }}{{ .PackageManagerIcon }} {{ else }}{{end}} {{ .Full }}{{if .Mismatch }} 󰦎 {{ .Expected }}{{ end }} "
        properties:
          fetch_package_manager: true
          yarn_icon: ""
          npm_icon: ""
          pnpm_icon: "󰣙"
      - type: python
        style: plain
        powerline_symbol: 
        foreground: "#43CCEA"
        background: transparent
        template: "<#ffffff> </>{{ if .Error }}{{ .Error }}{{ else }}{{ if .Venv }}{{ .Venv }} {{ end }} {{ .Full }}{{ end }}"
        properties:
          home_enabled: false
          fetch_virtual_env: true
          display_default: true
          fetch_version: true
          display_mode: context
      - type: project
        style: plain
        foreground: "#43CCEA"
        background: transparent
        foreground_templates:
          - "{{ if .Error }}#ff0000{{ end }}"
          - '{{ if eq .Type "node" }}#6CA35E{{ end }}'
          - '{{ if eq .Type "cargo" }}#ffc933{{ end }}'
          - '{{ if eq .Type "dotnet" }}#5C2D91{{ end }}'
          - '{{ if eq .Type "python" }}#FFD43B{{ end }}'
          - '{{ if eq .Type "php" }}#8892BF{{ end }}'
          - '{{ if eq .Type "nuspec" }}#004880{{ end }}'
          - '{{ if eq .Type "julia" }}#A270BA{{ end }}'
          - '{{ if eq .Type "powershell" }}#539EB5{{ end }}'
        template:
          "<#ffffff> </>{{ if .Error }}{{ .Error }}{{ else }}{{ if .Version }}
          {{.Version}}{{ end }} {{ if .Name }}{{ .Name }}{{ end }}{{ end }} "
      - background: transparent
        foreground: "#ffffff"
        properties:
          style: dallas
          threshold: 0.0
        style: plain
        template: "<#ffffff> </>{{ .FormattedMs }}s "
        trailing_diamond: 
        type: executiontime
    type: prompt
  - alignment: right
    segments:
      - background: transparent
        foreground: "#43CCEA"
        properties:
          time_format: 3:04:05 PM
        style: plain
        template: " {{ .CurrentDate | date .Format }} "
        type: time
        leading_diamond: 
    type: prompt

  - alignment: left
    newline: true
    segments:
      - foreground: "#cccccc"
        foreground_templates:
          - "{{ if gt .Code 0 }}#ff0000{{ end }}"
          - "{{ if eq .Code 0 }}#00ff0d{{ end }}"
        properties:
          always_enabled: true
        style: plain
        template: "{{if eq .Code 0}}󰅂{{else}}󰖭{{end}}"
        type: status
    type: prompt
final_space: true
version: 2.0
